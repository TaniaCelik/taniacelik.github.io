{
  "name": "garys-fencing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@8.15.6",
  "scripts": {
    "get-photos": "node src/getPhotos.js && node src/createThumbnails.js",
    "dev": "npm run get-photos && vite",
    "build": "npm run get-photos && vite build",
    "preview": "npm run get-photos && vite preview"
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "vite": "^5.4.10"
  },
  "dependencies": {
    "motion": "^10.18.0",
    "sharp": "^0.33.5"
  }
}
